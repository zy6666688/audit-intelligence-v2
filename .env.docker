# ================================
# 审计底稿引擎 - Docker部署环境变量
# ================================

# 数据库配置
DB_NAME=audit_engine
DB_USER=postgres
DB_PASSWORD=AuditEngine2024!
DB_PORT=5432

# Redis配置
REDIS_PASSWORD=Redis2024!
REDIS_PORT=6379

# 后端配置
NODE_ENV=production
BACKEND_PORT=3000
JWT_SECRET=audit-engine-secret-key-2024-change-this-in-production-min-32-chars
JWT_EXPIRES_IN=7d
UPLOAD_DIR=./uploads
BASE_URL=http://localhost:3000

# 前端配置
FRONTEND_PORT=80
API_BASE_URL=http://localhost:3000/api

# ================================
# 以下配置为可选配置
# ================================

# 企业微信配置 (可选)
# VITE_WXWORK_APPID=ww1234567890abcdef

# 千问API配置 (可选)
# VITE_QWEN_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxx

# 对象存储配置 (可选)
# VITE_OSS_BUCKET=audit-files
# VITE_OSS_REGION=oss-cn-hangzhou
# VITE_OSS_ACCESS_KEY_ID=LTAI5xxxxxxxxxxxxx
# VITE_OSS_ACCESS_KEY_SECRET=xxxxxxxxxxxxxxxxxxxxx
