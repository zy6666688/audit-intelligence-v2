# 📝 代码注释规范

**更新时间**: 2025-12-03  
**适用范围**: 所有TypeScript/JavaScript代码

---

## 🎯 目标

- ✅ **可读性**: 让代码易于理解和维护
- ✅ **文档化**: 自动生成API文档
- ✅ **团队协作**: 统一注释风格
- ✅ **长期维护**: 确保注释的准确性和时效性

---

## 📚 JSDoc标准

我们采用**JSDoc标准**编写注释，配合TypeScript类型系统使用。

### 基本格式

```typescript
/**
 * 函数简要描述（一句话）
 * 
 * @description 详细描述（可选，多行说明）
 * @param {类型} 参数名 - 参数说明
 * @returns {类型} 返回值说明
 * @throws {异常类型} 异常说明
 * @example 使用示例
 */
function example(param: string): string {
  // ...
}
```

---

## 🏷️ 常用标签

### 必需标签

#### @param - 参数说明
```typescript
/**
 * @param {string} name - 用户名称
 * @param {number} [age=18] - 年龄（可选，默认18）
 * @param {Object} options - 配置选项
 * @param {boolean} options.enabled - 是否启用
 */
```

#### @returns - 返回值
```typescript
/**
 * @returns {Promise<User>} 用户对象Promise
 * @returns {void} 无返回值
 */
```

#### @throws - 异常说明
```typescript
/**
 * @throws {BusinessError} 当用户不存在时
 * @throws {ValidationError} 当参数无效时
 */
```

### 可选但推荐的标签

#### @description - 详细描述
```typescript
/**
 * 创建新用户
 * 
 * @description 在数据库中创建新用户记录
 * 自动生成唯一ID和创建时间
 * 发送欢迎邮件（异步）
 */
```

#### @example - 使用示例
```typescript
/**
 * @example
 * ```typescript
 * const user = await createUser({
 *   name: '张三',
 *   email: 'zhang@example.com'
 * });
 * console.log(user.id); // 'user-123'
 * ```
 */
```

#### @since - 添加版本
```typescript
/**
 * @since v1.2.0
 * @since Week 2 - 核心节点开发
 */
```

#### @author - 作者
```typescript
/**
 * @author SHENJI Team
 * @author Zhang San <zhangsan@example.com>
 */
```

#### @date - 日期
```typescript
/**
 * @date 2025-12-03
 */
```

### 高级标签

#### @template - 泛型类型
```typescript
/**
 * @template T - 数据类型
 * @param {T} data - 输入数据
 * @returns {Promise<T>} 处理后的数据
 */
async function process<T>(data: T): Promise<T> {
  // ...
}
```

#### @deprecated - 已废弃
```typescript
/**
 * @deprecated 使用 newFunction() 代替
 * @see {@link newFunction}
 */
function oldFunction() {
  // ...
}
```

#### @see - 参考链接
```typescript
/**
 * @see {@link ResponseFormatter}
 * @see https://example.com/docs
 */
```

#### @remarks - 备注
```typescript
/**
 * @remarks
 * 注意事项：
 * - 该方法仅在开发环境可用
 * - 需要管理员权限
 * - 会修改全局状态
 */
```

---

## 📋 注释层级

### 1. 文件级注释

**每个文件**开头必须有文件级注释：

```typescript
/**
 * 统一API响应格式工具类
 * 
 * @description 提供统一的API响应格式化方法
 * @author SHENJI Team
 * @date 2025-12-03
 * @version 1.0.0
 * @since Week 1: API优化
 * 
 * @example
 * ```typescript
 * import { ResponseFormatter } from './ResponseFormatter';
 * return ResponseFormatter.success(data);
 * ```
 */
```

### 2. 类级注释

```typescript
/**
 * 审计节点抽象基类
 * 
 * @description 所有审计节点的基类
 * @abstract
 * 
 * @remarks
 * 子类必须实现execute方法
 * 
 * @example
 * ```typescript
 * export class CustomNode extends BaseNode {
 *   async execute(inputs, config) {
 *     // ...
 *   }
 * }
 * ```
 */
export abstract class BaseNode {
  // ...
}
```

### 3. 方法/函数注释

```typescript
/**
 * 创建成功响应
 * 
 * @description 用于返回操作成功的标准响应格式
 * 
 * @template T - 响应数据类型
 * @param {T} data - 响应数据
 * @param {string} [message='操作成功'] - 提示消息
 * @returns {ApiResponse<T>} 格式化的响应对象
 * 
 * @example
 * ```typescript
 * const user = { id: 1, name: 'John' };
 * return ResponseFormatter.success(user, '查询成功');
 * // { code: 200, success: true, data: {...}, ... }
 * ```
 */
static success<T>(data: T, message: string = '操作成功'): ApiResponse<T> {
  // ...
}
```

### 4. 接口/类型注释

```typescript
/**
 * API响应接口
 * 
 * @description 定义统一的API响应格式
 * 
 * @template T - 响应数据类型，默认为any
 * 
 * @interface
 */
export interface ApiResponse<T = any> {
  /** HTTP状态码 */
  code: number;
  
  /** 操作是否成功 */
  success: boolean;
  
  /** 响应数据 */
  data: T | null;
  
  /** 提示消息 */
  message: string;
  
  /** 时间戳（毫秒） */
  timestamp: number;
  
  /** 错误详情（仅开发环境） */
  error?: any;
}
```

### 5. 枚举注释

```typescript
/**
 * 错误码枚举
 * 
 * @description 系统中所有错误的数字编码
 * @enum {number}
 */
export enum ErrorCode {
  /** 参数错误 - 客户端请求参数不正确 */
  INVALID_PARAMS = 1001,
  
  /** 缺少参数 - 客户端请求缺少必需参数 */
  MISSING_PARAMS = 1002,
  
  // ...
}
```

---

## ✅ 最佳实践

### 1. 简洁明了

```typescript
// ✅ 好的注释
/**
 * 验证用户邮箱格式
 * @param {string} email - 邮箱地址
 * @returns {boolean} 格式是否有效
 */
function validateEmail(email: string): boolean {
  // ...
}

// ❌ 冗余注释
/**
 * 这个函数用于验证用户的电子邮件地址格式是否符合标准的邮箱格式规范
 * 它会检查邮箱地址中是否包含@符号以及是否有有效的域名
 * @param {string} email - 这是需要验证的用户电子邮件地址字符串参数
 * @returns {boolean} 返回一个布尔值，表示邮箱格式是否有效
 */
```

### 2. 提供示例

所有复杂方法和公共API都应提供示例：

```typescript
/**
 * 生成审计底稿
 * 
 * @example
 * ```typescript
 * const path = await generateWorkpaper(
 *   '固定资产盘点底稿',
 *   [{
 *     title: '盘点结果',
 *     headers: ['资产编号', '资产名称', '状态'],
 *     data: [{ 资产编号: 'A001', 资产名称: '电脑', 状态: '正常' }]
 *   }]
 * );
 * ```
 */
```

### 3. 说明副作用

```typescript
/**
 * 删除用户
 * 
 * @remarks
 * ⚠️ 该操作不可逆！会同时删除：
 * - 用户的所有工作流
 * - 用户的所有审计记录
 * - 用户的上传文件
 * 
 * @param {string} userId - 用户ID
 * @returns {Promise<void>}
 */
```

### 4. 标注异步操作

```typescript
/**
 * 发送邮件通知
 * 
 * @async
 * @param {string} email - 收件人邮箱
 * @param {string} subject - 邮件主题
 * @returns {Promise<void>}
 * 
 * @throws {BusinessError} 当邮件服务不可用时
 */
async function sendEmail(email: string, subject: string): Promise<void> {
  // ...
}
```

### 5. 使用TypeScript类型

TypeScript已提供类型信息，注释应补充语义信息：

```typescript
// ❌ 冗余
/**
 * @param {string} name - name是一个字符串
 */
function greet(name: string) {
  // ...
}

// ✅ 有价值
/**
 * @param {string} name - 用户显示名称（2-20个字符）
 */
function greet(name: string) {
  // ...
}
```

---

## 🚫 避免的做法

### 1. 无用注释

```typescript
// ❌ 没有提供额外信息
/**
 * 获取用户
 * @param {string} id - id
 * @returns {User} user
 */
function getUser(id: string): User {
  // ...
}

// ✅ 提供有价值的信息
/**
 * 根据ID查询用户详细信息
 * @param {string} id - 用户唯一标识符
 * @returns {User} 包含完整信息的用户对象
 * @throws {BusinessError} 当用户不存在时 (ErrorCode.NOT_FOUND)
 */
function getUser(id: string): User {
  // ...
}
```

### 2. 过时的注释

```typescript
// ❌ 注释与代码不符
/**
 * 返回用户名称
 */
function getUserEmail(id: string): string {
  // 实际返回的是邮箱...
}

// ✅ 保持同步
/**
 * 返回用户邮箱地址
 */
function getUserEmail(id: string): string {
  // ...
}
```

### 3. 代码即注释

```typescript
// ❌ 翻译代码为中文
// 如果用户年龄大于18
if (user.age > 18) {
  // 设置成年标志为真
  isAdult = true;
}

// ✅ 说明为什么
// 成年用户才能访问受限内容
if (user.age > 18) {
  isAdult = true;
}
```

---

## 🔧 工具支持

### TypeDoc

生成API文档网站：

```bash
cd packages/backend
npm run docs
```

配置文件：`typedoc.json`

### ESLint

强制注释规范（计划中）：

```bash
npm run lint
```

### VSCode插件

推荐安装：
- **Document This**: 自动生成JSDoc模板 (`Ctrl+Alt+D` 两次)
- **Better Comments**: 高亮特殊注释
- **JSDoc Markdown highlighting**: JSDoc语法高亮

---

## 📊 注释检查清单

提交代码前，检查以下项目：

### 文件级
- [ ] 文件开头有文件级注释
- [ ] 包含@author和@date
- [ ] 包含@description说明文件用途

### 类级
- [ ] 所有导出的类有类级注释
- [ ] 包含@description
- [ ] 抽象类标注@abstract

### 方法级
- [ ] 所有public方法有完整注释
- [ ] 所有@param都有说明
- [ ] @returns说明返回值
- [ ] 可能抛出异常的标注@throws
- [ ] 复杂方法提供@example

### 类型级
- [ ] 所有导出的接口/类型有注释
- [ ] 接口属性有行内注释或JSDoc
- [ ] 枚举值有行内注释

---

## 🎓 学习资源

- [JSDoc官方文档](https://jsdoc.app/)
- [TypeScript JSDoc参考](https://www.typescriptlang.org/docs/handbook/jsdoc-supported-types.html)
- [TypeDoc文档](https://typedoc.org/)
- [Google JavaScript风格指南](https://google.github.io/styleguide/jsguide.html)

---

## 📝 注释模板

### 快速模板

```typescript
/**
 * [简要描述]
 * 
 * @param {[类型]} [参数名] - [参数说明]
 * @returns {[类型]} [返回值说明]
 * 
 * @example
 * ```typescript
 * [使用示例]
 * ```
 */
```

### 完整模板

```typescript
/**
 * [简要描述]
 * 
 * @description [详细描述]
 * 
 * @param {[类型]} [参数名] - [参数说明]
 * @returns {[类型]} [返回值说明]
 * 
 * @throws {[异常类型]} [异常说明]
 * 
 * @example
 * ```typescript
 * [使用示例]
 * ```
 * 
 * @remarks
 * [备注说明]
 * 
 * @since [版本/时间]
 * @author [作者]
 */
```

---

**注释是代码的一部分，请认真对待！** 💯

---

*最后更新: 2025-12-03*  
*维护人: SHENJI Team*
