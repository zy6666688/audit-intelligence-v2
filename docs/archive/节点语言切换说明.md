# 🌍 节点语言切换功能说明

## 概述

审计数智析系统支持中文和英文两种语言，所有节点的标签、描述和分类都提供了完整的双语支持。

---

## 支持的语言

| 语言代码 | 语言名称 | 说明 |
|---------|---------|------|
| `zh` | 中文 | 默认语言，简体中文 |
| `en` | English | 英语 |

---

## 节点多语言信息

### 节点标签 (Label)

每个节点都有中英文标签：

```json
{
  "label": {
    "zh": "简单加法",
    "en": "Simple Add"
  }
}
```

### 节点描述 (Description)

每个节点都有中英文描述：

```json
{
  "description": {
    "zh": "将两个数字相加",
    "en": "Add two numbers together"
  }
}
```

### 节点分类 (Category)

分类也提供多语言支持：

| 分类代码 | 中文 | English |
|---------|------|---------|
| `utility` | 实用工具 | Utility |
| `input` | 数据输入 | Input |
| `transformation` | 数据转换 | Transformation |
| `audit` | 审计分析 | Audit |
| `output` | 数据输出 | Output |

---

## 所有节点的多语言标签

### 实用工具类 (Utility)

| 节点类型 | 中文名称 | English Name | 图标 |
|---------|---------|--------------|------|
| `simple_add` | 简单加法 | Simple Add | 🧮 |
| `simple_multiply` | 简单乘法 | Simple Multiply | ✖️ |
| `echo` | 回显节点 | Echo | 🔊 |

### 数据处理类 (Data Processing)

| 节点类型 | 中文名称 | English Name | 图标 |
|---------|---------|--------------|------|
| `data.csv_reader` | CSV读取 | CSV Reader | 📄 |
| `data.filter` | 数据过滤 | Filter | 🔍 |
| `data.map` | 数据映射 | Map | 🔄 |
| `data.aggregate` | 数据聚合 | Aggregate | 📊 |

### 审计分析类 (Audit)

| 节点类型 | 中文名称 | English Name | 图标 |
|---------|---------|--------------|------|
| `audit.data_compare` | 数据对比 | Data Compare | 📊 |
| `audit.amount_calculate` | 金额计算 | Amount Calculate | 💰 |
| `audit.sampling` | 审计抽样 | Audit Sampling | 🎲 |
| `audit.voucher_analysis` | 凭证分析 | Voucher Analysis | 📝 |
| `audit.invoice_validation` | 发票验证 | Invoice Validation | 🧾 |

---

## API 使用示例

### 获取节点列表（指定语言）

**请求**:
```http
GET /api/nodes?lang=zh
GET /api/nodes?lang=en
```

**中文响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "type": "simple_add",
      "label": "简单加法",
      "description": "将两个数字相加",
      "category": "实用工具",
      "icon": "🧮"
    }
  ]
}
```

**英文响应示例**:
```json
{
  "success": true,
  "data": [
    {
      "type": "simple_add",
      "label": "Simple Add",
      "description": "Add two numbers together",
      "category": "Utility",
      "icon": "🧮"
    }
  ]
}
```

---

## 前端集成示例

### 设置语言偏好

```typescript
// 设置用户语言偏好
const language = 'zh'; // 或 'en'
localStorage.setItem('language', language);

// 获取节点列表时传递语言参数
const response = await fetch(`/api/nodes?lang=${language}`);
const nodes = await response.json();
```

### 动态切换语言

```typescript
// 语言切换组件
function LanguageSwitcher() {
  const [lang, setLang] = useState('zh');
  
  const switchLanguage = (newLang: 'zh' | 'en') => {
    setLang(newLang);
    localStorage.setItem('language', newLang);
    // 重新加载节点数据
    loadNodes(newLang);
  };
  
  return (
    <div>
      <button onClick={() => switchLanguage('zh')}>中文</button>
      <button onClick={() => switchLanguage('en')}>English</button>
    </div>
  );
}
```

---

## 后端实现

### 国际化工具函数

```typescript
import { Language, getLocalizedText } from './utils/i18n';

// 获取本地化文本
const text = {
  zh: '简单加法',
  en: 'Simple Add'
};

const label = getLocalizedText(text, 'zh'); // "简单加法"
const labelEn = getLocalizedText(text, 'en'); // "Simple Add"
```

### 格式化节点信息

```typescript
import { formatNodeInfo, formatNodeList } from './utils/i18n';

// 单个节点
const formattedNode = formatNodeInfo(node, 'en');

// 节点列表
const formattedNodes = formatNodeList(nodes, 'zh');
```

---

## 字段翻译对照表

### 输入/输出字段常用术语

| 中文 | English |
|------|---------|
| 数据 | Data |
| 结果 | Result |
| 文件路径 | File Path |
| 字段名 | Field Name |
| 数值 | Value |
| 条件 | Condition |
| 配置 | Config |
| 选项 | Options |

### 配置选项常用术语

| 中文 | English |
|------|---------|
| 分隔符 | Delimiter |
| 编码格式 | Encoding |
| 是否包含表头 | Has Header |
| 跳过行数 | Skip Rows |
| 比较字段 | Compare Fields |
| 忽略大小写 | Ignore Case |
| 聚合函数 | Aggregation Function |
| 分组字段 | Group By Field |

### 状态和结果

| 中文 | English |
|------|---------|
| 成功 | Success |
| 失败 | Failed |
| 运行中 | Running |
| 待处理 | Pending |
| 已取消 | Cancelled |
| 低风险 | Low Risk |
| 中等风险 | Medium Risk |
| 高风险 | High Risk |

---

## 最佳实践

### 1. 保持一致性

✅ **使用标准术语**
- 相同的概念在所有节点中使用相同的翻译
- 参考本文档的术语对照表
- 避免同义词混用

✅ **格式规范**
- 中文使用全角标点
- 英文使用半角标点
- 数字和单位之间有空格

### 2. 提供上下文

✅ **描述要完整**
- 说明节点的主要功能
- 说明典型使用场景
- 提供必要的使用提示

### 3. 用户体验

✅ **语言切换流畅**
- 切换语言后立即生效
- 保持用户的语言偏好
- 提供明显的语言切换入口

---

## 常见问题

### Q: 如何添加新语言？

A: 在 `i18n.ts` 中添加新的语言代码，并为所有节点和术语提供翻译。

### Q: 节点描述太长怎么办？

A: 
- 标签保持简短（2-6个字）
- 描述控制在30字以内
- 详细说明放在文档中

### Q: 如何处理专业术语？

A:
- 审计专业术语保持准确性
- 可以在描述中提供英文原文
- 例如："审计抽样 (Audit Sampling)"

### Q: 默认语言是什么？

A: 系统默认使用中文 (zh)，用户可以通过设置切换为英文。

---

## 开发者指南

### 添加新节点时的多语言支持

```typescript
const newNode: NodeDefinition = {
  manifest: {
    type: 'my.new_node',
    version: '1.0.0',
    category: 'audit',
    
    // 必须提供中英文标签
    label: {
      zh: '我的新节点',
      en: 'My New Node'
    },
    
    // 必须提供中英文描述
    description: {
      zh: '这是一个新节点的描述',
      en: 'This is a description of the new node'
    },
    
    // ... 其他配置
  },
  
  execute: async (inputs, config, context) => {
    // 执行逻辑
  }
};
```

### 字段描述的多语言化

```typescript
inputsSchema: {
  data: {
    type: 'array',
    // 字段描述也应提供双语
    description: {
      zh: '输入数据数组',
      en: 'Input data array'
    },
    required: true
  }
}
```

---

## 更新日志

### v1.0.0 (2025-12-02)
- ✅ 实现完整的中英文双语支持
- ✅ 所有12个节点提供双语标签和描述
- ✅ 添加语言切换工具函数
- ✅ 提供API语言参数支持

---

**文档维护**: 审计数智析开发团队  
**最后更新**: 2025-12-02
